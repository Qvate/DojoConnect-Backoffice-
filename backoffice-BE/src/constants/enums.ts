export enum AppointmentStatus {
  Pending = "pending",
  Upcoming = "upcoming",
  Completed = "completed",
}

export enum AppointmentType {
  Physical = "physical",
  Online = "online",
}

export enum NotificationType {
  
}